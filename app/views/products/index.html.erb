<% provide(:title, 'All Shows') %>
<h2>Product Details</h2>

<dl class="dl-horizontal">
  <dt>Product/Device Name</dt>
    <dd>Settings</dd>
  <dt>Special Instructions</dt>
   <dd>Channels, options specific to each theatre</dd>
 <% if current_user.admin?  %>

 <% end %>
</dl>

<div align='center'>
<% if current_user.admin?  %>
  <%= link_to "Create", new_product_path(@product), class: "btn btn-medium btn-primary" %>
<% end %>

<table width = '60%'>
  <% @products.each do |products| %>
    <tr><%= render products %></tr>
  <% end %>
</table>
</div>